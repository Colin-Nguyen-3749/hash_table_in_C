// This function helps us find the next largest prime number,
// which is useful for helping us resize hash tables should they
// get too full or if they're too empty
int is_prime(const int x);
int next_prime(int x);

